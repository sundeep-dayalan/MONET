{
  "name": "monet",
  "private": true,
  "version": "0.0.0",
  "packageManager": "pnpm@10.17.0",
  "workspaces": [
    "client",
    "core",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo dev --parallel",
    "build": "turbo build",
    "test": "turbo test --parallel",
    "lint": "turbo lint --parallel",
    "test-e2e": "turbo test-e2e --filter=client...",
    "run-mock-server": "turbo run-mock-server"
  },
  "devDependencies": {
  }
}